process {
    withName: "PDF2DCM"{
        cpus = 1
        memory = { task.attempt * 4.GB }
        time = { task.attempt * 5.m }
        publishDir = [path: {"${params.output_dir}/"}, mode: 'copy']
    }
}